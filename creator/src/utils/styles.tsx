import { FontWeights } from "office-ui-fabric-react";

export const boldStyle = { root: { fontWeight: FontWeights.semibold } };
export const appStyle = {
  root: {
    margin: "0 auto",
    textAlign: "center",
    color: "#605e5c",
  },
};
